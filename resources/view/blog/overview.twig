{% extends '_partials/index.twig' %}

{% block head %}
    {{ parent() }}

    {{ css.inline('resources/css/blog.scss')|raw }}
{{ css.inline('resources/css/overview.scss')|raw }}
{% endblock %}

{% block content %}
{#    {% if (overviewTitle ?? null) == 'programming' %}#}
{#        {% include '_partials/ad_scout.twig' %}#}
{#    {% endif %}#}

    <div class="hero">
        <div class="wrapper">
            <h1>
                <span>
                    stitcher.io
                </span>
            </h1>

            <div class="author spotlight">
                <h3>
                    Generics in PHP
                </h3>

                <p>
                    I'm creating a four-part mini-series on <a href="https://www.youtube.com/watch?v=2o8A9AgccKs&list=PL0bgkxUS9EaKyOugEDffRzsvupBE2YEoD&index=4&ab_channel=BrentRoose">YouTube</a> about generics in PHP: what they are, how we can use them today, and what's possible in the future. Give it a look, don't forget to leave like!
                </p>

                <a href="https://www.youtube.com/watch?v=2o8A9AgccKs&list=PL0bgkxUS9EaKyOugEDffRzsvupBE2YEoD&index=4&ab_channel=BrentRoose" target="_blank" rel="noopener noreferrer">
                    <img src="/resources/img/static/generics-thumb-4.png" alt="The case for PHP generics">
                    <p><em class="center small">The case for PHP generics</em></p>
                </a>
            </div>
        </div>
    </div>

    <article class="blog">
{#        {% include '_partials/header_links.twig' %}#}

    </article>

    <div class="wrapper">

        <nav>
            {% for post in posts %}
                {% if post.hidden is not defined or post.hidden == false %}
                    <a href="{{ detailUrl ?? '/blog' }}/{{ post.id }}" {% if post.highlight is defined and post.highlight %}class="highlight"{% endif %}>
                        {% if post.date is defined %}
                            <span>
                                {{ post.date|date("Y-m-d") }}
                            </span>
                        {% endif %}

                        {% if post.title is defined %}
                            <span class="post-title">
                                {{ post.title }}
                            </span>
                        {% endif %}
                    </a>
                {% endif %}
            {% endfor %}
        </nav>
    </div>

    <footer>
        <nav>
            {% if _pagination.previous.url %}
                <a href="{{ _pagination.previous.url }}" class="prev cta cta-light">
                    Previous
                </a>
            {% endif %}

            {% if _pagination.next.url %}
                <a href="{{ _pagination.next.url }}" class="next cta cta-light">
                    Next
                </a>
            {% endif %}
        </nav>

        {% include '_partials/links.twig' %}
    </footer>
{% endblock %}
