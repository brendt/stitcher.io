<div class="p-4 rounded-2xl flex flex-col justify-end shadow-lg bg-blue-200">
    <span class="text-xl font-bold font-mono break-all md:break-keep">{{ $metric ?? '-' }}</span>
    <span>{{ $title ?? 'TODO' }}</span>
</div>